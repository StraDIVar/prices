%h2
  Товары

.actions
  = link_to 'Новый товар', new_good_path, class: "btn btn-primary"

%table.table
  - @goods.each do |good|
    %tr
      %td
        .well
          = render good

        .actions
          = link_to 'Просмотр', good, class: "btn"
          = link_to 'Редактировать', edit_good_path(good), class: "btn btn-success"
